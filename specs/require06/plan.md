# 模块化菜单

## 目标

- 由于HIS系统比较大，需要按模块配置菜单，完善原有的菜单功能，实现按模块配置菜单的功能。

## 需求

- 按模块配置菜单，实现按模块配置菜单的功能。
- 角色管理分配权限时，可以按模块配置菜单

## 设计

- 把模块管理放在菜单管理中，加个按钮并弹窗进行模块维护
- 在模块配置菜单的选项中，可以添加、删除、修改模块菜单。
- sys_dict_type添加一个记录，模块管理，type_code=module，数据保存在系统的字典表中sys_dict_data
- 分院套餐也需要进行处理
